import 'package:flutter/material.dart';

class AnsweredTextView extends StatelessWidget {
  const AnsweredTextView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("You answered this test"));
  }
}
