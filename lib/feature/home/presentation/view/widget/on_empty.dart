import 'package:flutter/cupertino.dart';

class OnEmpty extends StatelessWidget {
  const OnEmpty({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Your'e list is empty!"));
  }
}
